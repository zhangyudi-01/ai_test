# 数据推送类服务-明细数据-测试方案

| **版本** | 更新时间 | 作者 | 描述 |
| --- | --- | --- | --- |
| 1.0 | 2023-10-23 | 王玲玲 | 创建 |
| 1.1 | 2023-10-31 | 王玲玲 | 更新 clerk\_daily\_report 文件的时间范围 |
| 1.2 | 2023-11-13 | 王玲玲 | 完善测试点 |
| 1.3 | 2025-12-15 | 刘家孜 | 完善测试点 |

# 引言

## 测试目的

通过测试，尽可能发现软件中的错误，确保功能与需求一致。

## 预期读者

项目组成员

## 参考资料

| 类别 | 链接 |
| --- | --- |
| 需求 | [《北单运维管理平台PRD文档》](https://alidocs.dingtalk.com/api/doc/transit?dentryUuid=MyQA2dXW7zQGkx4jSwDAbrgo8zlwrZgb&queryString=utm_medium%3Ddingdoc_doc_plugin_card%26utm_source%3Ddingdoc_doc)<br>[《Vegas 2.0 系统对接需求（明细数据）》](https://alidocs.dingtalk.com/api/doc/transit?dentryUuid=np9zOoBVBQrX0kO5swzeAkogW1DK0g6l&queryString=utm_medium%3Ddingdoc_doc_plugin_card%26utm_source%3Ddingdoc_doc) |
| 原型 |  |
| 设计 |  |
| 测试 |  |

## 名词定义

| 名词 | 定义 |
| --- | --- |
| 功能测试 | 基于系统需求，输入数据在预期结果和实际结果之间进行测试，部分功能需要在黑盒测试的基础上，更进一步测试。 |
| 安全测试 | 检查系统对非法侵入渗透的防范能力。 |
| 性能测试 | 通过高并发检查系统是否满足性能需求 |

# 测试内容

## active文件

## 相关业务流

将北单系统中对应的营销活动配置信息的全量数据生成csv数据文件。

## 功能测试

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成active的csv文件 | 文件名称格式验证 | active{{yyyyMMdd}}.csv<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 字段固定传空 | RETURN\_PCT、<br>P\_COUNT\_NAME、<br>TOTO\_AMT1、<br>TOTO\_AMT2、<br>F\_ACTIVE\_ID、<br>TERM\_TEXT<br>这些字段在active表中都不存在 |  |
|  |  | 字段固定传0 | ACTIVE\_STATUS<br>以上字段在active表中都不存在 |  |
|  |  | 字段固定传1 | SELECT\_TYPE、<br>PRIZE\_TYPE<br>以上字段在active表中都不存在 |  |
|  |  | 数据库表数据为空时，csv文件只有表头 |  |  |
|  |  | 存在数据时，其他字段数据与数据库表active表字段一致 | 明细数据需求中3.1.1中除以上字段的其他所有字段 |  |

## country\_branch\_operater文件

## 相关业务流

将北单系统中对应的兑奖处用户信息的全量数据生成csv数据文件。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成country\_branch\_operater的csv文件 | 文件名称格式验证 | country\_branch\_operater{{yyyyMMdd}}.csv<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 数据库表数据为空时，csv文件只有表头 |  |  |
|  |  | 存在数据时，字段数据与数据库表country\_branch\_operater表数据一致 | 明细数据需求中3.1.2中所有字段 |  |

## game\_def文件

## 相关业务流

将北单系统中对应的游戏配置信息的全量数据生成csv数据文件。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成game\_def的csv文件 | 文件名称格式验证 | game\_def{{yyyyMMdd}}.csv<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 字段固定传空 | GAME\_TEMPLATE\_ID、<br>GAME\_BEGIN\_TIME、<br>GAME\_END\_TIME、<br>PKG\_NAME、<br>PLAYER\_SELECTION\_CNT、<br>RESULT\_CNT、<br>RULE\_DESC、<br>GAME\_DESC、<br>TERM\_DESC、<br>PARENT\_GAME\_ID、<br>ADVANCE\_END\_SALE\_INTERVAL、<br>POSTPONE\_BEGIN\_PAID\_INTERVAL、<br>TIME\_SERVICE\_START\_TIME、<br>RISK\_CTL、<br>ADD\_CHANCE\_MULTIPLE、<br>ADD\_PRZ\_MULTIPLE、<br>RESULT\_TIE\_FLAG、<br>TRUNC\_TYPE、<br>MAX\_SIM\_SALE\_DRAW\_CNT、<br>MAX\_DRAW\_SHOP\_SALE\_TICKET\_CNT、<br>IF\_REWARD<br>以上字段在game\_def表中都不存在 |  |
|  |  | 字段固定传0 | TIME\_FLAG、<br>ASCII\_OFFSET、<br>COMPAT\_FLAG<br>以上字段在game\_def表中都不存在 |  |
|  |  | 字段固定传1 | GAME\_VER<br>以上字段在game\_def表中都不存在 |  |
|  |  | 字段固定传2 | MATCHUP\_TYPE、<br>TEAM\_USE\_TYPE<br>以上字段在game\_def表中都不存在 |  |
|  |  | 字段固定传3 | SELECTION\_TYPE<br>以上字段在game\_def表中都不存在 |  |
|  |  | ADJ\_FUND调节基金累计值小数位保留六位，四舍五入 |  |  |
|  |  | COMM\_ID传game\_def的game\_id |  |  |
|  |  | 数据库表数据为空时，csv文件只有表头 |  |  |
|  |  | 存在数据时，其他字段数据与数据库表game\_def一致 | 明细数据需求中3.1.3中除以上字段的其他所有字段 |  |

## game\_selection\_template文件

## 相关业务流

将北单系统中对应的投注选项配置信息的全量数据生成csv数据文件。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成game\_selection\_template的csv文件 | 文件名称格式验证 | game\_selection\_template{{yyyyMMdd}}.csv<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 字段固定传空 | SELECTION\_RULE<br>以上字段在<br>game\_selection\_template表中都不存在 |  |
|  |  | 字段固定传1 | GAME\_VER<br>以上字段在<br>game\_selection\_template表中都不存在 |  |
|  |  | 数据库表数据为空时，csv文件只有表头 |  |  |
|  |  | 存在数据时，其他字段数据与数据库表game\_selection一致 | 明细数据需求中3.1.4中除以上字段的其他所有字段 |  |

## loc文件

## 相关业务流

将北单系统中对应的地区信息的全量数据生成csv数据文件。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成loc的csv文件 | 文件名称格式验证 | loc{{yyyyMMdd}}.csv<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 字段固定传0 | DEL\_FLAG<br>以上字段在数据库表中均不存在 |  |
|  |  | 字段传 rdc\_id 对应的 old\_rdc\_id | P\_LOC\_ID |  |
|  |  | 数据库表数据为空时，csv文件只有表头 |  |  |
|  |  | 存在数据时，其他字段数据与数据库表loc数据一致 | 明细数据需求中3.1.5中所有字段 |  |

## nation文件

## 相关业务流

将北单系统中对应的国家信息的全量数据生成csv数据文件。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成nation的csv文件 | 文件名称格式验证 | nation{{yyyyMMdd}}.csv<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 字段数据与V1线上旧数据一致 | 明细数据需求中3.1.6中所有字段 |  |

## rdc文件

## 相关业务流

将北单系统中对应的彩票中心信息的全量数据生成csv数据文件。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成rdc的csv文件 | 文件名称格式验证 | rdc{{yyyyMMdd}}.csv<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 字段固定传空 | SHOP\_DEFAULT\_INIT\_QUOTA、<br>BI\_SUSPEND\_BEGIN\_DATE、<br>BI\_SUSPEND\_BEGIN\_TIME、<br>BI\_SUSPEND\_END\_DATE、<br>BI\_SUSPEND\_END\_TIME、<br>RDC\_BRANCH\_TYPE<br>以上字段在rdc表中都不存在 |  |
|  |  | 字段固定传1 | BANK\_INT\_TYPE、<br>RDC\_COMM\_TYPE、<br>RDC\_AWARD\_TYPE<br>以上字段在rdc表中都不存在 |  |
|  |  | RDC\_ID对应V1中心ID | 对应rdc表的old\_rdc\_id |  |
|  |  | 数据库表数据为空时，csv文件只有表头 |  |  |
|  |  | 存在数据时，其他字段数据与数据库表rdc数据一致 | 明细数据需求中3.1.7中除以上字段的其他所有字段 |  |

## rdc\_operater文件

## 相关业务流

将北单系统中对应的彩票中心用户信息的全量数据生成csv数据文件。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成rdc\_operater的csv文件 | 文件名称格式验证 | rdc\_operater{{yyyyMMdd}}.csv<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | RDC\_ID对应V1中心ID | 对应rdc\_operator表的old\_rdc\_id |  |
|  |  | 数据库表数据为空时，csv文件只有表头 |  |  |
|  |  | 存在数据时，其他字段数据与数据库表rdc\_operator一致 | 明细数据需求中3.1.8中所有字段 |  |

## toto\_dict文件

## 相关业务流

将北单系统中对应的数据字典信息的全量数据生成csv数据文件。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成toto\_dict的csv文件 | 文件名称格式验证 | toto\_dict{{yyyyMMdd}}.csv<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 数据库表数据为空时，csv文件只有表头 |  |  |
|  |  | 存在数据时，所有字段数据与数据库表toto\_dict表数据一致 | 明细数据需求中3.1.9中所有字段 |  |

## cancel\_ticket文件

## 相关业务流

将北单系统中对应的以下信息的增量数据生成csv数据文件。

文件说明：取消票信息。只传取消时间在统计日期，且取消成功的票信息。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成cancel\_ticket的csv文件 | 文件名称格式验证 | cancel\_ticket{{yyyyMMdd}}.csv<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 字段固定传空 | ADD\_FLAG<br>以上字段在cancel\_ticket表中都不存在 |  |
|  |  | 字段转换为可读格式 | SELECTION |  |
|  |  | 数据库无对应数据 | 导出文件内容只有表头信息 |  |
|  |  | 只导出取消时间在统计日期，且取消成功的票信息 | 正例：两个条件须同时满足<br>反例：两个条件都不满足；两个条件均只满足一个 |  |
|  |  | 数据库表数据为空时，csv文件只有表头 |  |  |
|  |  | 存在数据时，字段数据参考数据库表的cancel\_ticket表 | 明细数据需求中3.2.1中所有字段 |  |

## clerk\_daily\_report文件

## 相关业务流

将北单系统中对应的以下信息的增量数据生成csv数据文件。

文件说明：销售员日统计信息。只传报表日期大于等于统计日期减 30 天的信息。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成clerk\_daily\_report的csv文件 | 文件名称格式验证 | clerk\_daily\_report{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 数据库无对应数据 | 导出文件内容只有表头信息 |  |
|  |  | 只导出报表日期大于等于统计日期减30天的信息 | 分别准备报表日期大于、等于、小于统计日期减30天的数据 |  |
|  |  | 以clerk\_id、rpt\_date、game\_id进行分组，不同term\_id的数据进行求和 | 数据库表clerk\_term\_daily\_report中是以clerk\_id、rpt\_date、game\_id、term\_id作为主键的。 |  |
|  |  | COMM\_ID传game\_id的值 | 传clerk\_term\_daily\_report的game\_id的值 |  |
|  |  | 数据库表数据为空时，csv文件只有表头 |  |  |
|  |  | 存在数据时，字段数据取自数据库表clerk\_term\_daily\_report且符合规则 | 明细数据需求中3.2.2中所有字段 |  |

## ticket文件

## 相关业务流

将北单系统中对应的以下信息的增量数据生成csv数据文件。

文件说明：销售票信息。只传售票时间在统计日期的票信息。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成ticket的csv文件 | 数据多时产生多个文件 | 通过修改配置文件的分页，一页最多m万条，m根据数据可以自定义 |  |
|  |  | 无数据时，只会存在一个csv文件 |  |  |
|  |  | 无数据时，csv文件只有表头 |  |  |
|  |  | 文件名称格式验证 | ticket{n}{{yyyyMMdd}}<br>其中n为生成的ticket的第几个文件，从0开始，yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 字段固定传空 | ADD\_FLAG<br>以上字段在ticket表中都不存在 |  |
|  |  | 数据库无对应数据 | 导出文件内容只有表头信息 |  |
|  |  | 字段转换为可读格式 | SELECTION |  |
|  |  | 售票时间在统计日期内 | 边界值测试 |  |
|  |  | 字段数据与数据库一致 | 明细数据需求中3.2.3中除以上字段的其他所有字段 |  |

## undo\_ticket文件

## 相关业务流

将北单系统中对应的以下信息的增量数据生成csv数据文件。

文件说明：回滚票信息。只传回滚时间在统计日期，且回滚成功的票信息。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成undo\_ticket的csv文件 | 文件名称格式验证 | undo\_ticket{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 字段固定传空 | SALE\_DRAW\_LIST、<br>PAID\_DRAW\_LIST、<br>RENEW\_CLERK\_ID、<br>OLD\_TICKET\_NO、<br>ADD\_FLAG<br>以上字段在<br>undo\_ticket表中都不存在 |  |
|  |  | 字段固定传0 | PAID\_DRAW\_CNT、<br>CONFIRM\_FLAG、<br>以上字段在<br>undo\_ticket表中都不存在 |  |
|  |  | 字段固定传1 | GAME\_VER、<br>SALE\_DRAW\_CNT、<br>TOTAL\_DRAW\_CNT、<br>UNDO\_TYPE、<br>以上字段在<br>undo\_ticket表中都不存在 |  |
|  |  | 字段转换为可读格式 | SELECTION |  |
|  |  | 数据库无对应数据 | 导出文件内容只有表头信息 |  |
|  |  | 只导出回滚时间在统计日期内，且回滚成功的票信息 | 反例：两个条件都不满足；两个条件均只满足一个 |  |
|  |  | 无数据时，csv文件只有表头 |  |  |
|  |  | 字段数据取自数据库表undo\_ticket且符合规则 | 明细数据需求中3.2.4中除以上字段的其他所有字段 |  |

## draw\_ticket文件

## 相关业务流

多期票信息。

按字段要求生成空数据文件并上传。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成draw\_ticket的csv文件 | 文件名称格式验证 | draw\_ticket{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 只有字段名称，字段数据都为空 | 明细数据需求中3.3.1中所有字段 |  |

## his\_draw\_ticket文件

## 相关业务流

历史多期票信息。

按字段要求生成空数据文件并上传。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成his\_draw\_ticket的csv文件 | 文件名称格式验证 | {{his\_draw\_ticket}}yyyyMMdd<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 只有字段名称，字段数据都为空 | 明细数据需求中3.3.2中所有字段 |  |

## kzc\_ticket文件

## 相关业务流

快中彩游戏销售票信息。

按字段要求生成空数据文件并上传。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成kzc\_ticket的csv文件 | 文件名称格式验证 | kzc\_ticket{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 只有字段名称，字段数据都为空 | 明细数据需求中3.3.3中所有字段 |  |

## ticket\_prize文件

## 相关业务流

销售票促销活动奖品明细。

按字段要求生成空数据文件并上传。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成ticket\_prize的csv文件 | 文件名称格式验证 | ticket\_prize{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 只有字段名称，字段数据都为空 | 明细数据需求中3.3.4中所有字段 |  |

## shop文件

## 相关业务流

实体店信息csv数据文件。

拥有相同 shop\_no 的实体店只上传最新的一条记录，即优先取启用且未删除状态的实体店对应的信息，若无，则取 shop\_id 最大的实体店对应的信息。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成shop的csv文件 | 文件名称格式验证 | shop{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 固定传空 | EMAIL、<br>FAX、<br>ZIP\_CODE、<br>CERTIFICATE\_TYPE、<br>CERTIFICATE\_NO、<br>MOBILE、<br>BRANCH\_ID<br>以上字段在shop表中都不存在 |  |
|  |  | 固定传0 | AVAIL\_QUOTA、<br>CREDIT\_QUOTA、<br>SALE\_COMM\_PCT、<br>PAID\_COMM\_PCT、<br>TEMP\_QUOTA\_LIMIT、<br>SHOP\_PRINT\_STATUS、<br>SHOP\_COMM\_TYPE<br>以上字段在shop表中都不存在 |  |
|  |  | 固定传1 | SHOP\_TYPE、<br>SHOP\_BANK\_INT\_TYPE<br>以上字段在shop表中都不存在 |  |
|  |  | 固定传6 | SHOP\_CATEGORY<br>以上字段在shop表中都不存在 |  |
|  |  | SHOP\_ID | 对应v2的shop\_no<br>shop\_no和shop\_id区分开 |  |
|  |  | RDC\_ID | 根据shop表的rdc\_id去rdc表取对应的old\_rdc\_id |  |
|  |  | 相同shop\_no优先取启用且未删除状态的实体店对应的信息 | 相同shop\_no下存在多条数据，启用未删除，以及非启用未删除 |  |
|  |  | 相同shop\_no无启用未删除状态店时，取shop\_id最大的实体店信息 | 相同shop\_no下存在多条数据，没有启用未删除的，有多条其他状态的数据 |  |
|  |  | 无数据时，csv文件只有表头 |  |  |
|  |  | 字段数据取自数据库shop且符合规则 | 明细数据需求中3.4.1中除以上字段的其他所有字段 |  |

## term文件

## 相关业务流

终端信息csv数据文件。

只传 18 中实体店下的终端记录。

存在重复的 term\_no 时，优先取未撤销且未删除状态的终端对应的信息，若无，则取 term\_id 最大的实体店对应的信息。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成term的csv文件 | 文件名称格式验证 | term{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 固定传空 | TERM\_IP<br>以上字段在term表中都不存在 |  |
|  |  | 固定传1 | UPDATABLE\_FLAG<br>以上字段在term表中都不存在 |  |
|  |  | 固定传999 | SOFT\_ID<br>以上字段在term表中都不存在 |  |
|  |  | TERM\_ID | 对应 V2 的 term\_no |  |
|  |  | SHOP\_ID | 对应 V2 的 shop\_no |  |
|  |  | 只导出属于本次查询的shop下的终端信息 | term表有属于本次查询的shop下的终端，也有不属于本次查询的shop下的终端 |  |
|  |  | 相同term\_no优先取未撤销且未删除状态的终端对应的信息 | 相同term\_no下存在多条数据，未撤销未删除(只会存在一条)，以及非未撤销未删除 |  |
|  |  | 相同term\_no且无未撤销且未删除状态的终端时，则取 term\_id 最大的实体店对应的信息 | 准备正例和反例 |  |
|  |  | 无数据时，csv文件只有表头 |  |  |
|  |  | 字段数据取自数据库表shop且符合规则 | 明细数据需求中3.4.2中除以上字段的其他所有字段 |  |

## clerk文件

## 相关业务流

销售员信息csv数据文件。

只传 18中实体店下的销售员记录。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成clerk的csv文件 | 文件名称格式验证 | clerk{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 固定传空 | LOGIN\_SERVER\_ID、<br>CERTIFICATE\_NO、<br>RDC\_PAY\_FLAG<br>以上字段在clerk表中都不存在 |  |
|  |  | 固定传0 | AVAIL\_QUOTA、<br>CERTIFICATE\_TYPE、<br>WEB\_FLAG<br>以上字段在clerk表中都不存在 |  |
|  |  | SHOP\_ID | V1 实体店 ID，对应 V2 的 shop\_no |  |
|  |  | 只导出属于本次查询的shop下的销售员信息 | clerk表有属于本次查询的shop下的销售员记录，也有不属于本次查询的shop下的销售员记录 |  |
|  |  | 三个字段取实体店下最大终端ID对应的数据 | CANCEL\_LIMIT、<br>PAY\_LIMIT、<br>CANCEL\_TIMEOUT |  |
|  |  | 无数据时，csv文件只有表头 |  |  |
|  |  | 字段数据取自数据库表clerk且符合规则 | 明细数据需求中3.4.3中除以上字段的其他所有字段 |  |

## win\_ticket文件

## 相关业务流

中奖票信息。

只传统计日期内发生开奖和兑奖行为的奖期的所有中奖票信息。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成win\_ticket的csv文件 | 文件名称格式验证 | win\_ticket{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 固定传空 | WINNER\_NAME、<br>CERTIFICATE\_TYPE、<br>CERTIFICATE\_NO、<br>ADD\_FLAG、<br>WINNER\_ID<br>以上字段在win\_ticket表中都不存在 |  |
|  |  | 字段转换为可读格式 | SELECTION |  |
|  |  | 发生开奖和兑奖行为的奖期的所有中奖票信息 | 注意是奖期，只要有票在统计日期内发生开奖和兑奖了，这个票所属的奖期下的所有的中奖票信息都要查出来 |  |
|  |  | 无数据时，csv文件只有表头 |  |  |
|  |  | 字段数据取自数据库表win\_ticket且符合规则 | 明细数据需求中3.4.4中除以上字段的其他所有字段 |  |

## win\_ticket\_prize文件

## 相关业务流

中奖票促销活动奖品明细csv文件。

只传统计日期内未止兑的所有中奖票促销活动数据。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成win\_ticket\_prize的csv文件 | 文件名称格式验证 | win\_ticket\_prize{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 统计日期内未止兑的所有中奖票 | 准备正例和反例 |  |
|  |  | 无数据时，csv文件只有表头 |  |  |
|  |  | 字段数据取自数据库表win\_ticket\_prize且符合规则 | 明细数据需求中3.4.5中所有字段 |  |

## game\_draw文件

## 相关业务流

奖期信息csv数据文件。

只传奖期开奖时间在统计日期前 3 个月内的数据。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成game\_draw的csv文件 | 文件名称格式验证 | game\_draw{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 固定传空 | ORIG\_RESULT、<br>DRAW\_LICENSE\_QUOTA、<br>MAIN\_LEAGUE\_NAME、<br>IF\_REWARD<br>以上字段在win\_ticket表中都不存在 |  |
|  |  | 固定传1 | GAME\_VER<br>以上字段在game\_draw表中都不存在 |  |
|  |  | 忽略V2暂停取消状态 | V1 奖期暂停状态，包含：0、奖期正常，1、销售暂停，兑奖正常，2、销售正常，兑奖暂停，3、销售暂停，兑奖暂停（V1）<br>V2中有7个，注意做好对应 |  |
|  |  | DRAW\_RESULT字段取自schedule表对应result | 与schedule表的draw\_id联立取开奖draw\_flag=1的result，schedule相同draw\_id下的多条记录先根据matchup\_no排序，再用~进行连接，-1用\*替代 |  |
|  |  | 奖期开奖时间在统计日期前 3 个月内的数据 |  |  |
|  |  | 无数据时，csv文件只有表头 |  |  |
|  |  | 字段数据取自数据库表game\_draw且符合规则 | 明细数据需求中3.4.6中除以上字段外的其他所有字段 |  |

## game\_draw\_stats\_clerk\_paid文件

## 相关业务流

销售员场次兑奖统计信息csv数据文件。

只传 23 中对应奖期的数据。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成game\_draw\_stats\_clerk\_paid的csv文件 | 文件名称格式验证 | game\_draw\_stats\_clerk\_paid{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 只导出属于本次查询的game\_draw下的销售员场次兑奖统计信息 | 准备正例和反例 |  |
|  |  | 无数据时，csv文件只有表头 |  |  |
|  |  | 字段数据取自数据库表game\_draw\_stats\_clerk\_paid且符合规则 | 明细数据需求3.4.7中所有字段 |  |

## game\_draw\_stats\_clerk\_sale文件

## 相关业务流

销售员奖期统计信息。

只传 24 中对应奖期的数据。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成game\_draw\_stats\_clerk\_sale的csv文件 | 文件名称格式验证 | game\_draw\_stats\_clerk\_sale{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 只导出属于本次查询的game\_draw下的销售员奖期统计信息 | 准备正例和反例 |  |
|  |  | 无数据时，csv文件只有表头 |  |  |
|  |  | 字段数据取自数据库表game\_draw\_stats\_clerk\_sale且符合规则 | 明细数据需求3.4.8中除以上字段以外其他所有字段 |  |

## matchup文件

## 相关业务流

赛程信息csv数据文件。

只传 25中对应奖期的数据。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成matchup的csv文件 | 文件名称格式验证 | matchup{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 固定传空 | SELECTION\_CNT<br>以上字段在matchup表中都不存在 |  |
|  |  | 只导出属于本次查询的game\_draw下的赛程信息 | 准备属于和不属于本次查询的game\_draw的信息 |  |
|  |  | MATCHUP\_TIMESTAMP时间戳 | 转更新时间为时间戳，用UNIX\_TIMESTAMP方法 |  |
|  |  | 无数据时，csv文件只有表头 |  |  |
|  |  | 字段数据取自数据库表schedule且符合规则 | 明细数据需求3.4.9中除以上字段以外其他所有字段 |  |

## team文件

## 相关业务流

球队信息csv数据文件

只传26中的赛程里使用的球队数据。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成team的csv文件 | 文件名称格式验证 | team{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 固定传空 | NATION\_ID<br>以上字段在team表中都不存在 |  |
|  |  | 只导出属于本次查询的赛程下的球队数据 | 注意主客队<br>准备正例和反例 |  |
|  |  | 无数据时，csv文件只有表头 |  |  |
|  |  | 字段数据取自数据库表team且符合规则 | 明细数据需求3.4.10中除以上字段以外其他所有字段 |  |

## league文件

## 相关业务流

赛事信息。

只传 27中的球队所属的赛事数据。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成league的csv文件 | 文件名称格式验证 | league{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 固定传0 | HIDE\_FLAG<br>以上字段在team表中都不存在 |  |
|  |  | 固定传3 | SELECTION\_TYPE<br>以上字段在team表中都不存在 |  |
|  |  | 只导出属于本次查询的球队下的赛事数据 | 注意主客队，准备正例和反例 |  |
|  |  | 无数据时，csv文件只有表头 |  |  |
|  |  | 字段数据取自数据库表league且符合规则 | 明细数据需求中3.4.11除以上字段外的其他所有字段 |  |

## total\_record文件

## 相关业务流

汇总信息。

统计同批上传的各文件的数据条数。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 生成total\_record的csv文件 | 文件名称格式验证 | total\_record{{yyyyMMdd}}<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 文件内容的表名字段正确 | 28个 |  |
|  |  | ticket数据总数为所有ticket表数据的总和 | 准备分页 |  |
|  |  | 任何一个表的数据为空时，该表对应的数据总数为0 |  |  |
|  |  | 文件内容符合规则且正确 | 符合明细数据需求中3.5的需求 |  |

## 自动上传

## 相关业务流

通过定时任务每天自动将明细数据文件上传至sftp服务器。

同时也上传至本地指定目录下。

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 文件自动上传 | 压缩包名称格式验证 | BJ\_{{yyyyMMdd}}.zip<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 上传时间正确 | k8s的定时任务只要调用该服务，上传的就是当前时间昨天的数据 |  |
|  |  | 文件本地上传路径 | 北单明细文件（文件夹）-{{yyyyMMdd}}（文件夹）-BJ\_{{yyyyMMdd}}（文件夹）和 BJ\_{{yyyyMMdd}}.zip包 - csv |  |
|  |  | 文件名称均正确 | 包含了以上1-29中所有文件 |  |

## 手动上传

## 相关业务流

通过修改配置文件将指定日期的明细数据上传至sftp服务器。

同时也上传至本地指定目录下

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 文件手动上传 | 压缩包名称格式验证 | BJ\_{{yyyyMMdd}}.zip<br>其中yyyyMMdd为当前时间前一天的时间 |  |
|  |  | 上传时间正确 | 通过修改配置文件上传指定日期的文件 |  |
|  |  | 文件本地上传路径 | 北单明细文件（文件夹）-{{yyyyMMdd}}（文件夹）-BJ\_{{yyyyMMdd}}（文件夹）和 BJ\_{{yyyyMMdd}}.zip包 - csv |  |
|  |  | 文件名称均正确 | 包含了以上1-29中所有文件 |  |

## 可配置项

## 测试功能

| 序号 | 测试项 | 测试点 | 备注 | 对应需<br>求列表 |
| --- | --- | --- | --- | --- |
|  | 可配置项 | ticket分页条数 | 可配置一个文件x万条，x为可配置项，测试一个文件和多个文件 |  |
|  |  | 删除N天前的本地文件 | 根据统计数据日期来计算的，假设数据统计日期为11.15日，(注意是数据所属的日期)，删除1天前的本地文件，即保留11.15日和11.14日的文件 |  |
|  |  | 国家中心文件路径 | 测试配置是否生效，至少准备两个不同的路径 |  |
|  |  | 本地文件路径 | 测试配置是否生效，至少准备两个不同的路径 |  |
|  |  | 手动执行时间 | 只能生成当前日期的昨天以及昨天以前的数据，注意准备正例和反例 |  |