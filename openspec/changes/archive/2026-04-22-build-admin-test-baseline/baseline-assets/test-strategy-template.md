# 测试方案模板

## 1. 使用边界

- 首版全量测试基线构建：本模板用于定义资产建设范围、覆盖原则与工作模式，不填充真实业务用例内容
- 后续迭代差异分析：本模板用于承接差异项、规则项、权限项，生成本次版本的测试范围和精准覆盖契约

## 2. 基本信息

| 字段 | 内容 |
| --- | --- |
| 方案 ID | `PLAN-<release>` |
| 发布/迭代标识 | `<版本号或 Sprint>` |
| 工作模式 | `<首版全量测试基线构建 / 后续迭代差异分析>` |
| 引用基线版本 | `<首版基线标签或最近一次结构层基线版本>` |
| 来源类型 | `<PRD/Story/Bug/Config/...，首版阶段可留空>` |
| 来源 ID/回指 | `<需求单号/缺陷单号/任务号，首版阶段可留空>` |
| 来源摘要 | `<一句话说明本次方案来源，首版阶段可留空>` |
| 负责人 | `<QA owner>` |
| 评审对象 | `<产品/开发/业务/测试>` |
| 关联差异项 | `<DIFF-...，首版阶段可留空>` |

## 3. 测试目标

- `<本次测试要证明什么>`
- `<哪些高风险问题必须被发现>`

## 4. 精准覆盖目标

| 覆盖对象 | 输入来源 | 覆盖要求 | 允许排除条件 |
| --- | --- | --- | --- |
| 关键规则 | `<RULE-...>` | `<至少一个 TC 或明确排除原因>` | `<当前版本未启用/环境阻塞/已降级接受>` |
| 高风险差异项 | `<DIFF-...>` | `<必须进入方案范围并具备覆盖状态>` | `<需审批接受>` |
| 权限敏感场景 | `<R-... / P-...>` | `<角色或权限维度必须在用例中体现>` | `<无>` |

## 5. 测试范围

### 5.1 In Scope

| 来源类型 | 来源 ID/回指 | 模块 ID | 角色 ID | 权限项 ID | 规则 ID | 差异项 ID | 覆盖主题 | 计划用例 ID | 计划自动化项 ID | 覆盖方式 | 优先级 | 覆盖状态 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `<PRD/Story/Bug/Config/...>` | `<需求单号/缺陷单号/任务号>` | `<M-...>` | `<R-...>` | `<P-...>` | `<RULE-...>` | `<DIFF-...>` | `<主流程/边界/异常/权限/规则>` | `<TC-...，可多个>` | `<AUTO-...，如适用>` | `<手工/自动化/探索性>` | `<P0/P1/P2>` | `<待设计/待执行/已覆盖/部分覆盖/未覆盖>` | `<补充说明>` |

### 5.2 Out of Scope

| 对象类型 | 对象 ID | 不覆盖原因 | 风险等级 | 审批/接受方式 |
| --- | --- | --- | --- | --- |
| `<RULE/DIFF/P/...>` | `<RULE-... / DIFF-... / P-...>` | `<环境未就绪/需求冻结/低风险等>` | `<高/中/低>` | `<接受风险/后续补测/重新评审>` |

## 6. 测试重点与策略

| 风险主题 | 风险说明 | 测试策略 |
| --- | --- | --- |
| `<权限风险>` | `<例如不同角色同页差异大>` | `<角色对比 + 接口鉴权 + 数据范围验证>` |

## 7. 环境与数据

| 类别 | 内容 |
| --- | --- |
| 环境 | `<测试环境/联调环境/预发环境>` |
| 配置要求 | `<开关、模板、账号、依赖服务>` |
| 数据准备 | `<基础数据、脏数据、边界数据>` |
| 外部依赖 | `<短信、支付、消息、第三方接口等>` |

## 8. 准入 / 准出

### 8.1 Entry Criteria

- `<代码提测完成>`
- `<关键配置可用>`
- `<依赖服务联通>`

### 8.2 Exit Criteria

- `<P0/P1 用例执行完成>`
- `<阻塞缺陷关闭或评估接受>`
- `<高风险差异项完成验证>`
- `<未覆盖项有明确原因和后续动作>`
- `<DIFF/RULE/TC/AUTO 追踪链可被抽样审计>`

## 9. 排期与交付物

| 阶段 | 时间 | 输出 |
| --- | --- | --- |
| 差异分析 | `<日期>` | `<迭代差异分析>` |
| 用例设计 | `<日期>` | `<测试用例>` |
| 自动化评估 | `<日期>` | `<自动化映射>` |
| 执行与回归 | `<日期>` | `<执行记录>` |
| 测试总结 | `<日期>` | `<测试报告>` |

## 10. 备注

- 涉及权限或规则的测试主题，建议明确绑定 `角色 ID`、`权限项 ID`、`规则 ID`
- 若工作模式为“首版全量测试基线构建”，本模板只描述资产建设范围，不填充真实业务用例内容
